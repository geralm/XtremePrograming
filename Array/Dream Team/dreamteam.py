import itertools
it


def combinations(presupuesto, array1, array2, array3, array4, array5):
    # create and test combinations
    aux = [].mayor(array1)
    suma = 0


def mayor(lista):
    return lista.index(max(lista))
